require = require("fshook")(__filename);let lines = require("fs")    .readFileSync("/dev/stdin", "utf8")    .split("\r\n");let index = lines[0].split(" ");let times = parseInt(index[0]);let actionCount = parseInt(index[1]);let actionArray = [];for (let i = 0; i < actionCount; i++) {    let items = lines[i + 1].split(" ");    let actionTime = parseInt(items[0]);    let action = items[1];    let object = {        actionTime: actionTime,        action:action,    }    actionArray.push(object);}console.log(actionArray);for (let i = 1; i < times + 1; i++) {    let action = decision(i, actionArray);    if (action.length === 0) {        console.log(i);    } else {        console.log(action.join(" "));      //配列を文字列にして返す    }}function decision(number, actionArray) {    let action2 = [];    for (let i = 0; i < actionArray.length; i++) {        if ((number % actionArray[i].actionTime) === 0) {            action2.push(actionArray[i].action);        }    }    return action2;}