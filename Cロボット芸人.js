require = require("fshook")(__filename);let lines = require("fs")    .readFileSync("/dev/stdin", "utf8")    .split("\r\n");let index = lines[0].split(" ");let time = parseInt(index[0]);let actionCount = parseInt(index[1]);let actionObject = {};for (let i = 0; i < actionCount; i++) {    let actionArray = lines[i + 1].split(" ");    let number = actionArray[0];    let move = actionArray[1];    actionObject[number] = move;}for (let i = 0; i < time; i++) {    console.log(actionObject[i + 1]);}