require = require("fshook")(__filename);let lines = require("fs")    .readFileSync("/dev/stdin", "utf8")    .split("\r\n");let index = lines[0].split(" ");let times = parseInt(index[0]);let actionCount = parseInt(index[1]);for (let i = 1; i < times + 1; i++) {    decision (i);}function decision(time) {    for (let j = 0; j < actionCount; j++) {        let actionArray = lines[j + 1].split(" ");        let actionTime = parseInt(actionArray[0]);        let action = actionArray[1];        if ((i % actionTime) === 0) {            console.log(action);        }    }}